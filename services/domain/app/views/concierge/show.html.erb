<% content_for :title, "Concierge — Atlas" %>

<div class="mk-detail-head">
  <div>
    <h1>Concierge</h1>
    <span class="mk-count">One conversation across Voice · SMS · Email · Chat — with live intent triaging.</span>
  </div>
  <%= button_to "Start over", concierge_reset_path, method: :post, class: "mk-btn mk-btn--ghost" %>
</div>

<%= render "console", conversation: @conversation %>
