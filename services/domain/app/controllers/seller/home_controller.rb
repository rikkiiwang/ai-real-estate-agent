module Seller
  # Seller workspace landing. Requires a signed-in visitor. The valuation +
  # cash-offer flow is added in a later unit; this is the workspace entry point.
  class HomeController < Consumer::BaseController
    def show; end
  end
end
