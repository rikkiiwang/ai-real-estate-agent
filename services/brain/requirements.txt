# Brain service (Python): agents, valuation, vision, lawyer, RAG.
# U1 needs only gRPC + test tooling; heavier deps (lightgbm, langgraph,
# pgvector, ragas, google-genai) are added by the units that introduce them.
grpcio>=1.74
grpcio-tools>=1.74
protobuf>=5.27
pytest>=8.0
# U3 valuation engine: gradient-boosting AVM over a hermetic synthetic dataset.
scikit-learn>=1.4
numpy>=1.26
# U5 RAG source-of-truth store: pgvector-backed prod store (lazily-connecting;
# tests use the in-memory store + a deterministic fake embedder, no DB needed).
pgvector>=0.3
psycopg[binary]>=3.1
# U10 orchestrator: LangGraph state graph wiring generate -> critique -> rail ->
# decide with a checkpointer for resumability. Tests use the in-process
# MemorySaver (no DB); prod swaps in a PostgresSaver for restart-durable runs.
langgraph>=0.2
