[project]
name = "brain"
version = "0.1.0"
description = "AI Real Estate Agent — Brain service (valuation, vision, lawyer, RAG, orchestrator)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
